{
  "$schema": "https://dtif.lapidist.net/schema/core.json",
  "easing": {
    "invalid": {
      "$type": "easing",
      "$value": {
        "easingFunction": "cubic-bezier",
        "parameters": [1.2, 0.1, 0.5, 1]
      }
    }
  }
}
