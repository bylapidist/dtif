---
---

Clarify registry-related documentation claims to reflect current enforcement boundaries (registry metadata is documented and consumed by tooling/docs, while schema validation remains independent of registry allow-list enforcement).
