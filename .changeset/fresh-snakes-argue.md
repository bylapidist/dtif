---
"@lapidist/dtif-schema": patch
---

Enforce `$overrides` resolution-source exclusivity so each override uses exactly one of `$ref`, `$value`, or `$fallback`, matching the DTIF theming specification.
