---
'@lapidist/dtif-parser': patch
---

Emit consumer-facing warnings when tokens use unrecognised `$type` identifiers, using the canonical schema type set to keep parser diagnostics aligned with DTIF conformance requirements.
