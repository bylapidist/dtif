---
'@lapidist/dtif-schema': patch
---

Allow reverse-DNS extension identifiers with hyphenated labels (for example `com.example-ui.tokens`) so schema validation matches DTIF extension namespace rules.
