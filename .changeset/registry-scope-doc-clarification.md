---
'@lapidist/dtif-language-server': patch
---

Clarify registry integration claims: `$type` completions are registry-backed, while `$extensions` namespace suggestions come from reverse-DNS snippets and observed workspace keys.
