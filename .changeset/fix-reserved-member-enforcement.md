---
'@lapidist/dtif-schema': patch
---

Reject unrecognised reserved (`$*`) members in component values, cursor object payloads, cursor parameters, and function-parameter object literals to align with conformance and extensibility rules.
