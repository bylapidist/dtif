---
"@lapidist/dtif-validator": patch
---

Add semantic conformance checks for override inline values, function parameter alias typing, and typography nested reference typing so spec-invalid documents are rejected by the validator.
